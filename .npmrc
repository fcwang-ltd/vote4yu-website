# pnpm settings for this repo.
# Do not block scripts on esbuild/sharp (Astro's optimiser toolchain).
# verify-deps-before-run=false stops pnpm from running an install-check before
# each `pnpm run`, which otherwise hard-fails on the ignored-builds warning.
verify-deps-before-run=false

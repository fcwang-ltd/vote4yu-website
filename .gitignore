# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
.output/

# Astro
.astro/

# Environment / secrets
.env
.env.*
!.env.example

# OS / editor
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/

# Local scratch
tmp/
*.local

# ---------------------------------------------------------------------------
# Cursor handoff plans (gitignored on purpose)
# docs/plans/ holds the internal build handover pulled from the vault.
# It is SENSITIVE (live municipal campaign strategy) and must NOT ship in the
# public repo. Tracked locally for the coding agent, never committed/pushed.
# ---------------------------------------------------------------------------
docs/plans/

# ---------------------------------------------------------------------------
# Plaintext for the password-gated unlisted page.
# Only the ENCRYPTED payload is ever built into dist/. The readable source
# below must never be committed: this repository is PUBLIC, and anything
# committed here stays in git history after deletion.
# ---------------------------------------------------------------------------
private/

# ---------------------------------------------------------------------------
# Canvassing address data. York Region civic addresses for real households —
# fetched at runtime by the app, never committed. This repository is PUBLIC and
# git history is permanent, so a snapshot committed once cannot be taken back.
# ---------------------------------------------------------------------------
public/canvass/

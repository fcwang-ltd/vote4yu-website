# The unlisted volunteer tooling is kept out of search by its own
# noindex,nofollow,noarchive meta tag and excluded from the sitemap in
# astro.config.mjs. Deliberately NOT listed here: robots.txt is world-readable,
# so a Disallow line naming the path would publish the URL it protects.
User-agent: *

Sitemap: https://vote4yu.ca/sitemap-index.xml
